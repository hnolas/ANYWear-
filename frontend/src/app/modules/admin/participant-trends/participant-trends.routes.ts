import { Routes } from '@angular/router';
import { ParticipantTrendsComponent } from './participant-trends.component';

const routes: Routes = [
    {
        path: '',
        component: ParticipantTrendsComponent,
    },
];

export default routes;
